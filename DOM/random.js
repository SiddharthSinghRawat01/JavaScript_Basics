function whosPaying(names) {
    names = ["sid","anf","siddd"]
    var numberOfPeople = names.length;
    var randomPersonPosition = Math.floor(Math.random()*
        numberOfPeople);
    var randomPersonPosition = name[randomPersonPosition]
    return randomPersonPosition + "is going to lunch"
}
